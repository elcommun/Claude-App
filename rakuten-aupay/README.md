# 楽天→auPAY 商品データ作成ツール

楽天RMSの商品データ（`dl-normal-item.csv`）から、auPAYマーケットにアップロードできる `item.csv` / `stock.csv` を丸ごと生成するシングルページアプリ。**入力は楽天のCSVのみ**で、全商品が新規登録（ctrlCol=n、lotNumber空欄）として出力される。

変換ルールはauPAYの実データ（item.csv / stock.csv のサンプル、921商品の対応関係）から導き出したものを内蔵している。**ルールの追加・変更はユーザーからの依頼に応じてこのアプリを更新する**（auPAYファイルの読み込み機能はない）。

既存の `aupay-convert/`（auPAYの登録済みデータを楽天データで部分修正するツール）とは別アプリ。

## 入力ファイル（種類はヘッダーから自動判別・まとめてドロップ可）

| ファイル | 必須 | 用途 |
|---|---|---|
| 楽天 `dl-normal-item.csv` | ✅ | 商品情報・SKU・価格・在庫・画像・オプション |
| 楽天 `dl-item-cat.csv` | 任意 | 店舗内カテゴリ（shopCategory1〜10） |

auPAYの item.csv / stock.csv をドロップした場合は「使用しません」と案内して無視する。楽天商品CSVがヘッダー行のみ（商品データ0件）の場合はエラーとして明示し、案内する（例: rakuten-filterで絞り込んだ結果が0件になっているケースなど）。

## 主な変換ルール

- **商品名**: 先頭装飾（＼P2倍＋クーポン／等）を削除。【限定】【Raffinart】【APJ】は保持
- **価格**: 表示価格（定価）優先で固定（表示価格がないSKUは通常購入販売価格）
- **在庫**: すべて0で出力（実在庫はauPAY側で設定する運用）。楽天で倉庫指定（非公開）の商品は除外
- **1商品1価格**: auPAYは1商品ページに複数価格を載せられないため、SKUの価格が複数ある場合は**多数派の価格グループのSKUだけを残す**（例: 1200円×2、1500円×3 → 1200円×2のSKUを削除）。同数の場合は**低価格側を残す**（例: 1200円×2、1500円×2 → 1500円×2のSKUを削除）。削除内容は警告一覧に表示
- **説明文**: キャッチコピー→description。スマホ用商品説明文→descriptionForSP、**同じ内容をdescriptionForPCにも設定**（PC用販売説明文は使用しない）。改行除去、先頭の「&lt;!-- ビーノ：商品説明 --&gt;」等のテンプレコメントを除去（連続していれば繰り返し除去）、`image.rakuten.co.jp/elcommun/cabinet` → `image.wowma.jp/69585078/elcommun/cabinet` 書き換え、商品ページリンクは `/u/69585078/c/品番` に変換、楽天カテゴリリンク（`/c/数字/`）は内蔵の対応表（241件）で `/user/69585078/shopcategory/ID` に変換。対応が無いカテゴリリンク・変換できない楽天リンクはアンカー解除して警告
- **カテゴリ**: ジャンルID→auPAYカテゴリIDは実績データから抽出した122件のマッピングを内蔵（未対応は警告）
- **店舗内カテゴリ**: 楽天カテゴリURL→auPAY店舗内カテゴリ（shopCategoryName / shopCategoryId）の対応表（241件、ユーザー提供CSVから生成）で照合し、auPAY側の正式カテゴリ名で出力。対応が無いカテゴリは除外して警告一覧に表示。「セール商品」は黙って除外、親階層の重複も除外、最大10件
- **配送方法**: 楽天の配送方法セット管理番号＋送料区分1の組み合わせから deliveryMethodId/Name を自動設定
- **商品オプション名の変換**（「■」の後の空白有無は無視して照合）:
  - 「■ 送料無料をご希望の場合」→「■「送料無料」をご希望の場合」
  - 「■ ご購入前に「メール便」注意事項をご確認下さい」→「■「メール便」注意事項をご購入前にご確認下さい」
  - 「■ メール便「対象外商品」と同梱の場合」→「■「メール便・対象外商品」と同梱の場合」
- **バリエーション在庫**: SKUごとに stock.csv 行を生成（横軸 choicesStockHorizontal、`&`→`/` 変換）。Codeは楽天の「SKU管理番号」列をそのまま使用（例: 選択肢名「flower（JYX-001）」／SKU管理番号「JYX-001」→ Code「JYX-001」）。SKU管理番号に不可視の空白文字が混入している場合は通常の空白に正規化。書式が崩れている（空白や小数点を含む）SKU管理番号は警告一覧に表示。choicesStockShippingDayIdは空欄で出力
- **固定値**: taxSegment=3 / reducedTax=1 / postageSegment=1 / searchTarget=1 ほか（auPAY実績データ921商品の値と一致確認済み）

## 出力

- `item.csv`（253列）・`stock.csv`（16列）、auPAYダウンロード形式と同一ヘッダー
- 文字コードは Shift-JIS（既定・アップロード用）/ UTF-8 を選択
- ctrlCol はすべて `n`（新規登録）、lotNumber は空欄、在庫数はすべて `0`
- item.csv / stock.csv は個別ダウンロードのほか、両方をまとめた `item_stock.zip` も一括ダウンロード可能（`zip-tool` と同じ JSZip を使用。ファイル名は半角英数字のみのため文字化けの心配なし）

## 商品画像のダウンロード（`laikle-convert` と同方式）

変換結果に含まれる商品から、①商品画像パス・白背景画像パス・SKU画像パス等（「画像」かつ「パス/URL」を含む列すべて）と、②キャッチコピー・PC/スマートフォン用商品説明文・PC用販売説明文・共通説明文内に`<img>`タグとして直接埋め込まれている画像（`s-image-`系など）の両方から、楽天CABINET内の画像パスを収集し、一括ダウンロード用のターミナルコマンドを生成する。

- 変換完了後に表示される「商品画像をダウンロードする」カードの「📋 画像DLコマンドをコピーする」ボタンを押すと、実行コマンドがクリップボードにコピーされる（ファイル保存は不要）
- ターミナルに貼り付けて実行すると、`curl`（Referer: https://www.rakuten.co.jp/ を付与）で画像を1件ずつ取得し、`~/Downloads/aupay_images/` にCABINETと同じフォルダ構成のまま保存
- ダウンロード完了後、`main-image-7`・`sku-image` 等の各フォルダ（＝複数商品の画像が入るテンプレート単位の共有フォルダ）ごとに、そのフォルダ名のZIP（例: `main-image-7.zip`）を**フォルダの中に**自動作成する（`zip` コマンドが無い環境ではスキップして警告表示）。完了後はFinderが自動で開く
- auPAYはauPAY側サーバー（image.wowma.jp）の画像しか表示できないため、作成されたZIPをWow!managerの画像管理から同じフォルダ単位でアップロードする運用を想定
- コピーされるコマンドは常に「いま画面に表示されている最新の変換結果」の画像リストを含む（過去の実行結果は混ざらない）

## Wow!managerへの画像アップロード手順（FTP）

ダウンロードした画像ZIPをauPAY（Wow!manager）に反映させる手順を、「商品画像をダウンロードする」カードの次に「Wow!managerへ画像をアップロードする」カードとして表示する（画像0件のときは両カードとも非表示）。

- **FTP接続先**: ホスト名 `img.manager.wowma.jp`（画像管理専用サーバー・ポート21）。利用には「一括商品CSVオプション」（有料）の申し込みが必要
- ユーザーID／パスワードはWow!manager「アカウント管理・権限設定 > FTPパスワード変更」ページで確認・再設定
- ダウンロードしたZIPファイルを**展開せずそのまま**FTPソフト（Cyberduck等）でルートにアップロードする。zip内のフォルダ階層（`main-image-7`・`sku-image` 等、ハイフンを含む楽天の元の階層のままでOK）がそのままWow!manager側の画像ディレクトリとして反映される
- **反映まで実測30分程度かかる**（アップロード直後は画像管理画面に出てこないが正常。Wow!manager側の画面で反映確認、FTPソフト側では反映結果は分からない）
- フォルダ・画像の削除はWow!manager側からのみ可能
- カード内の「Wow!manager公式マニュアル原文を見る」の折りたたみに、FTPアップロードの正式な手順・注意事項（画像1枚2MB/1回2GBまで、gif/jpg/jpegのみ、フォルダ名は半角英数字のみ登録可能、zip形式のみ受付 等）を原文のまま掲載

## バージョン番号

`index.html` 内の `const APP_VERSION = N;` を**ファイル変更のたびに必ず1つ増やすこと**（5分ごとにサーバーと比較して自動リロード／更新通知）。
